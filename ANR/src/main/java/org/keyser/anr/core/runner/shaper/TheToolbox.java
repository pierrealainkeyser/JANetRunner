package org.keyser.anr.core.runner.shaper;


//@CardDef(name = "The Toolbox", oid = "01041")
public class TheToolbox 
//extends Hardware 
{

	//private WalletRecuringCredits wrc = new WalletRecuringCredits(null, this, (v) -> v instanceof UseIceBreaker, 2, RunnerStartOfTurnEvent.class);
	//
	// public TheToolbox() {
	// super(Faction.SHAPER.infl(2), Cost.credit(9));
	//
	// registerPrevent(match(RunnerInstalledCleanup.class).pred(this::equals).call(this::install));
	// registerPrevent(match(CardTrashedEvent.class).pred(this::equals).call(this::uninstall));
	// }
	//
	// private void install() {
	// RunnerOld runner = getGame().getRunner();
	// runner.alterMemory(2);
	// runner.getWallet().registerPrevent(wrc);
	// runner.alterLink(-2);
	// }
	//
	// private void uninstall() {
	// RunnerOld runner = getGame().getRunner();
	// runner.alterMemory(-2);
	// runner.getWallet().remove(wrc);
	// runner.alterLink(-2);
	// }

}
